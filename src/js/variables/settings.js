import APP_CONSTANTS from "./constants";

const defaultSettings = {
    QType: APP_CONSTANTS.QTypes.BASIC,
    includePractice: true,
    nQuestions: 15,
    nPractice: 3,
    paginate: true,
    itemsPerPage: 5,
};

export default defaultSettings;